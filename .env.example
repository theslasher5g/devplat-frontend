# Base URL of the devplat control-plane API.
# Local development falls back to http://localhost:3000 automatically;
# the production Docker build bakes in https://api.devplat.ch (see Dockerfile ARG).
VITE_API_URL=https://api.devplat.ch
